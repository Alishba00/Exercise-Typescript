//When username is empty.
let username :string[] = [];

if(username.length > 0 ){
        console.log(`Username is not Empty`);
    } else{
        console.log(`Username is empty`);
    }

//when username is not empty.
let username2 :string[] = ["Alishba","Atiqa"];

if(username2.length > 0 ){
        console.log(`Username is not Empty`);
    } else{
        console.log(`Username is empty`);
    }
