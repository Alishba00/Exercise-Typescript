"use strict";
function make_shirt(size, color, message) {
    console.log(`Customize a ${size} size t_shirt, in ${color} color with the message is ${message}`);
}
;
make_shirt("medium", "black", "Never give up!");
