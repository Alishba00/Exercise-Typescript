"use strict";
let car = {
    model: "e-tron",
    company: "Audi",
    year: 2022,
    color: "Black",
};
console.log(`The ${car.company} company introduce ${car.model} in ${car.year} which is available in ${car.color} color.`);
